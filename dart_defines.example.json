{
  "_comment": [
    "Template for local dart-define values. Copy to dart_defines.json",
    "(gitignored) and fill in real values for `flutter run`.",
    "",
    "Use it via:  flutter run --dart-define-from-file=dart_defines.json",
    "",
    "Mirrors what the GitHub Actions web deploy injects from repo",
    "secrets — except the public web build only ever gets the two",
    "Supabase values. Deepgram + OpenAI are real secrets and stay",
    "out of public bundles; they're only injected when you run on a",
    "phone or in a private TestFlight / Play Store build.",
    "",
    "OPENAI_API_KEY is no longer read by the client — every OpenAI",
    "call routes through the Supabase Edge Function `openai-chat` and",
    "the key lives in Supabase's secret store. Set it via:",
    "  supabase secrets set OPENAI_API_KEY=sk-...",
    "",
    "DEEPGRAM_API_KEY is still client-side because realtime",
    "transcription uses WebSockets and Edge Functions can't proxy",
    "those without a separate temp-token-grant pattern. It's only",
    "passed for native dev builds; the public web bundle ships",
    "without it (Env.hasDeepgram returns false there)."
  ],

  "SUPABASE_URL": "https://your-project.supabase.co",
  "SUPABASE_ANON_KEY": "eyJ...your-publishable-key...",

  "DEEPGRAM_API_KEY": "your-deepgram-key-here"
}
