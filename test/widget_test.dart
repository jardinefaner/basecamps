import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app renders', (tester) async {
    // Foundation smoke test — will be expanded with features.
    expect(1 + 1, equals(2));
  });
}
