SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Deepgram (voice-to-text in Observe). Dev-only; move behind a Supabase
# edge function before shipping a public build.
DEEPGRAM_API_KEY=

# OpenAI (automatic domain + sentiment tagging for observations).
# Dev-only; move behind a Supabase edge function before shipping.
OPENAI_API_KEY=
